/** Puzzle definitions. Placeholder — replaced by the content pass. */
import type { PuzzleDefinition, PuzzleId } from '@/engine/types';

export const puzzles: Record<PuzzleId, PuzzleDefinition> = {};
